# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_ALLOWED_USER_IDS=

# HackerOne (https://api.hackerone.com/getting-started-hacker-api/)
H1_USERNAME=
H1_API_TOKEN=

# Bugcrowd — public engagements/scopes (no login required)
# Optional: BUGCROWD_SESSION=_bugcrowd_session value only if you want private invites too
BUGCROWD_SESSION=

# YesWeHack (https://api.yeswehack.com/doc)
YESWEHACK_TOKEN=
YESWEHACK_TOTP_SECRET=

# Polling
POLL_INTERVAL_MINUTES=30
# Comma-separated: hackerone,bugcrowd,yeswehack
ENABLED_PLATFORMS=hackerone,bugcrowd,yeswehack

# Storage (10 GiB hard cap on SQLite)
DB_PATH=data/bugcontrol.db
DB_MAX_BYTES=10737418240
DB_SOFT_LIMIT_BYTES=8589934592
ARTIFACT_DIR=data/artifacts
ARTIFACT_MAX_BYTES_PER_JOB=52428800

# Scanners
SCAN_CONCURRENCY=2
SCAN_TIMEOUT_SECONDS=1800
# /secrets: live crawl + in-memory regex (no JS saved to disk)
SECRETS_CRAWL_MAX_PAGES=80
SECRETS_CRAWL_MAX_JS=500
SECRETS_CRAWL_DEPTH=3
SECRETS_CRAWL_TIMEOUT=15
SECRETS_MAX_JS_BYTES=2097152
SECRETS_CHUNK_BYTES=65536
SECRETS_OVERLAP_BYTES=2048
SECRETS_MAX_CONCURRENT=2
SECRETS_MAX_HITS=200
NMAP_BIN=nmap
SQLMAP_BIN=sqlmap
NIKTO_BIN=nikto

# Cursor cloud agents
CURSOR_API_KEY=
CURSOR_AGENT_REPO=https://github.com/usethisname1419/bugcontrol
CURSOR_MODEL=composer-2.5
CURSOR_AGENT_REF=main
# Auto-run bug-hunter agent after /secrets completes (requires CURSOR_*)
AI_AUTO_AFTER_SECRETS=true

# Logging
LOG_LEVEL=INFO
